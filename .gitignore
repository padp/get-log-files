# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
/__pycache__/
.claude/*
/.claude/*
*.claude/

# OS junk
Thumbs.db

# Internal press schedule files -- proprietary scheduling/operator data, never publish
/press_schedules/

# Camera snapshots -- internal facility images, never publish
collector/camera_snapshots/
collector/camera_snapshots_annotated/
collector/camera_snapshots_sample/
/camera_snapshots/

# Local-testing-only secrets copy (this network share's layout differs from the
# real collector PC's -- production secrets live outside the repo, at ../secret/)
/secret/